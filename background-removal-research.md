# 瀏覽器端去背模型研究紀錄

更新日期：2026-08-27

## 已排除方案

`@imgly/background-removal` 可在瀏覽器執行，但專案採 AGPL-3.0；在未確認 AbiPaint 的授權相容性前，不納入公開網站。

來源：[IMG.LY Background Removal JS](https://github.com/imgly/background-removal-js)

## 可驗證替代方案

IS-Net general-use 是 DIS 專案為一般用途釋出的高精度二值分割模型。上游 DIS 專案採 Apache-2.0 授權，模型包含一般用途版本；公開 ONNX 轉換亦標示 Apache-2.0。

量化的 `isnet-general-use-q8.onnx` 可用於瀏覽器 WASM，文件標示大小為 44,436,071 bytes，輸入為 `1 × 3 × 1024 × 1024`，正規化方式為 `(pixel - 128) / 256`，輸出為 `0..1` 顯著性遮罩。此模型比目前 U²-NetP 的 320 × 320 推論解析度高，但只應在使用者點擊去背時按需下載；需驗證手機記憶體與首次下載時間。

來源：[DIS 官方專案](https://github.com/xuebinqin/DIS)、[IS-Net general-use ONNX 模型卡](https://huggingface.co/SacredNoir/isnet-general-use-onnx)、[上游 ONNX 匯出](https://huggingface.co/x-Liola-x/isnet-general-use-onnx)

## 人像專用備選

MODNet 是 Apache-2.0 的 RGB-only 即時人像 matting 模型，適合人像髮絲與透明邊緣，但不適合作為 Logo、圖示與一般物件的唯一模型。若 IS-Net 一般模式完成後仍需改善頭像邊緣，可再以人像模式作為選用功能。

來源：[MODNet 官方專案](https://github.com/ZHKKKe/MODNet)

## 瀏覽器端探針

2026-08-27 已確認量化 IS-Net 的公開模型網址會回傳可跨網域讀取的最終下載端點，且檔案大小約 44 MB。瀏覽器探針已在初次下載後成功完成一次 1024 × 1024 WASM 推論：輸入名稱為 `input`，輸出包含 `output`，其遮罩張量形狀為 `1 × 1 × 1024 × 1024`。後續整合仍需保留明確的下載／處理進度與可回退錯誤訊息。

同日從 `https://abipaint.abiting.cc/` 公開網域測試，直接 `fetch` 該模型可取得 `200` 與 `cors` 回應；以 ONNX Runtime Web 1.27.0 建立 `wasm` session 也成功，模型輸入為 `input`，輸出清單含 `output`。因此使用者截圖中的 `Failed to fetch` 較可能是短暫 CDN／網路下載中斷，而非固定的公開網域 CORS 阻擋。正式流程需要在 session 建立失敗時重試一次，並提供恢復原先已載入模型的回退。

ONNX Runtime Web 官方文件指出，WASM 支援所有 ONNX operator；模型檔大時可用 IndexedDB 快取，並可視需求調整 execution provider 或以 worker 避免主執行緒阻塞。IS-Net 目前的 1024 × 1024 WASM 推論與大尺寸遮罩合成會佔用主執行緒，應將預處理、推論與遮罩合成移至專用 Web Worker；主畫面僅接收輸出 PNG／遮罩，避免處理期間拖慢畫布。

來源：[ONNX Runtime Web 部署指南](https://onnxruntime.ai/docs/tutorials/web/deploy.html)、[ONNX Runtime Web 教學](https://onnxruntime.ai/docs/tutorials/web/)

公開頁面的 Chromium 環境雖存在 `navigator.gpu`，但實測無法取得 WebGPU adapter，不能把 GPU 作為可靠的預設效能方案。去背效能改善需以 WASM 及 Web Worker 為基準，並只在 adapter 真正可用時再選擇 WebGPU。

2026-08-27 另從公開 AbiPaint 網域以 `ort.env.wasm.proxy = true` 實測量化 IS-Net：模型 session 可建立，且成功完成一次 `1 × 3 × 1024 × 1024` 輸入與 `1 × 1 × 1024 × 1024` 輸出推論。此設定可讓 ONNX Runtime 將 WASM 工作移出畫布主執行緒，是降低處理中工作台卡頓的安全預設。

## 已驗證的白描邊去除原則

2026-08-27 以白底動漫頭像實測後確認，單純縮小整個遮罩、依模型信心擴張細節，或只處理半透明像素，都無法可靠移除原圖本身帶有的白色描邊，並且容易傷及髮絲與臉部。

AbiPaint 的固定流程是：先保留模型產出的主體遮罩，再從圖片四個外邊緣開始，僅對白色／近白色的**來源像素**做四向連通追蹤；所有可從圖片外部連到的像素，都將對應遮罩改為透明。這直接移除貼在主體外圍的白描邊，但不會移除沒有與外側連通的白色眼睛、高光、衣物或其他主體細節。

「邊緣清理」控制只調整白色／近白色的判定容忍度；它不得改回全圖侵蝕、人物特例或依背景種類分支的規則。後續若調整去背，必須以白描邊頭像、白色主體細節與深色髮絲為回歸案例，確認這個外圍連通原則仍成立。

## 均勻有色背景的主體保護

2026-08-29 的淺藍背景貓咪案例顯示，原本的白／近白邊緣條件過寬時，可能把淺色有色背景視為白描邊的起點，並沿連通的白色臉部造成大面積誤刪。修正後先在 1024 × 1024 模型遮罩上辨識四周是否為穩定、有色且非近白的背景；若成立，先移除與該背景色相近的殘留遮罩，再回補與背景顏色、亮度或彩度明顯不同的低透明度主體像素。

外圍白描邊清理採中等近白範圍，能移除白底人物或 Logo 外緣殘留，又不把淺藍、淺灰等背景當成白色起點。此規則只作用於均勻有色背景，白底及複雜背景仍維持原有的模型遮罩、封閉小洞回補與外圍連通清理流程。

## 模型一致性

公開瀏覽器與內部環境不得因模型下載失敗而靜默切換到不同解析度的模型。高解析度模型經一次重試仍無法載入時，流程會保留原圖並直接提示使用者稍後重試，而不輸出品質不一致的結果。

然而，斑駁水彩或紙紋背景可能讓 IS-Net 的遮罩保留大量彼此分離的高不透明背景區。此時會在高品質 IS-Net 遮罩中量測前景覆蓋率與最大連通主體之外的像素比例；只有覆蓋率過高，或分離區超過前景 3.5% 時，才按需執行 320 × 320 的 U²-NetP 替代遮罩。若替代遮罩確實更緊湊且仍保留最小主體面積，才採用它。白底、單一主體與一般人像仍優先使用 1024 × 1024 IS-Net，避免把替代模型當成常態回退。

同時，公開 GitHub Pages 部署的 JavaScript 與 CSS 改為以內容雜湊命名。先前固定的 `chunk-main.js` 可能被瀏覽器或 CDN 長時間快取，造成頁面看似最新、實際卻執行舊版去背邏輯；內容雜湊可讓每版 HTML 指向唯一的對應程式資產。

2026-08-29 的公開瀏覽器錯誤顯示跨網域模型載入可能在重新導向期間失敗。載入流程現在優先使用 AbiPaint 的公開鏡像，並由 ONNX Runtime 直接以模型 URL 建立 session；避免 JavaScript 先持有完整模型 `ArrayBuffer`，以降低手機的記憶體尖峰。第一次失敗後延遲 700 ms 重試一次；兩次均失敗時保留原圖，使用者重新點擊去背即可再次嘗試。

同日晚間的手機 Safari 測試仍回報 `Failed to fetch`，因此模型主來源改為 AbiPaint 專案的公開靜態儲存鏡像。該鏡像會轉送至可跨網域讀取的 CloudFront 物件；原始 Hugging Face 連結只保留為相同 1024 × 1024 模型的第二來源。這避免手機瀏覽器必須直接處理 Hugging Face 的 Xet 重新導向鏈。

已從獨立的瀏覽器工作台頁面以與正式程式相同的 `fetch` 選項驗證鏡像：回應為 `200`，完整讀取大小為 `44,436,071` bytes，且回應具有跨網域讀取權限。此鏡像可作為公開手機瀏覽器的優先載入來源。

內部預覽環境另由除錯收集器攔截跨網域 `fetch`，即使公開鏡像可用，仍可能回報 `Failed to fetch`。當網域為內部 `.manus.computer` 預覽站時，模型改為先走同源 `/manus-storage/` 儲存代理；已驗證同源回應可完整讀取 `44,436,071` bytes。GitHub Pages、自訂公開網域與 Manus 公開網域不符合此條件，仍使用公開鏡像及 Hugging Face 備援。

手機 Chrome 以 768 × 768 執行同一高品質 IS-Net 模型，桌面與 Safari 仍為 1024 × 1024。這是降低行動 Chrome 推論輸入、輸出與遮罩合成同時配置所造成的記憶體風險，而不是回退到不同模型；去背完成後仍維持相同的遮罩清理與細節微調流程。

## 公開站資產核對

2026-08-29 重新開啟 `https://abipaint.abiting.cc/` 時，公開工作台載入的主程式網址為 `/assets/chunk-main.js`。這是固定檔名，排查公開與內部結果不同時，需額外確認該資產是否已包含最新去背邏輯，不能只依工作台頁面成功載入判定程式已同步。

同日以停用快取方式比對公開 `/assets/chunk-main.js` 與本地正式建置檔，其雜湊不同；公開檔含先前的邊緣條件標記，卻未含最新均勻背景主體保護的函式名稱。這表示外部自訂網域的資產更新與內部預覽可能不同步；後續回歸測試需以公開網域重新載入並確認實際程式標記。

目前自動化瀏覽器可辨識工作台的匯入按鈕與隱藏檔案欄位，但無法對該隱藏欄位完成檔案注入。因此，去背效果的最終視覺回歸仍須在公開站以原始素材手動匯入確認；建置與程式路徑可先由自動檢查保障。

## 成熟工具的主體判定研究

2026-08-29 查核 Bazaart 的公開說明後可確認：它將去背定位為 AI 自動偵測人物或物件，並明確提供橡皮擦作為自動去背後清除殘留的人工細修流程；官方沒有公開模型名稱或雲端／裝置端的推論架構。其產品覆蓋 Logo、自拍與商品圖，也支援多張與影片工作流程，顯示商用品質並非僅靠單一二值遮罩。

主體判定的業界通用解法是提示式分割：以使用者的正向點、負向點、方框或既有遮罩提示模型要保留的特定實例，再以邊緣 matting 估算半透明 alpha。Meta 的 Segment Anything 專案提供可依點選與框選產生物件遮罩的模式；此類「模型初判 + 使用者意圖 + 邊緣精修」流程，較適合處理多色 Logo、被主體包住的留白與多物件畫面。

## 修補遮罩試作

去背完成後直接進入「細節微調／Refine」。畫布呈現目前合成結果，修補筆從原始來源取回像素、移除筆清空像素；提交時系統比較修補前後的 alpha 與原始來源像素，將使用者真正保留／移除的區域回寫到 `backgroundMask`，再由原始圖片與更新後遮罩重新合成。此設計不會把修補直接烤進原圖，日後仍可調整邊緣柔化與邊緣清理，也可重新進入細節微調。細節微調期間的頁首復原與 `Ctrl/Cmd + Z` 都會優先回復最近一筆修補筆觸；一般工作檔歷程則在離開細節微調後恢復使用。

## 斑駁背景與檢視底色

斑駁背景未必符合四周顏色均勻的條件，故新增「外圍主色 + 低模型信心」的連通追蹤：僅從圖片邊界開始，沿著色彩接近外圍主色且信心低於 0.72 的像素清理。此規則針對藍色水彩、紙張紋理等背景殘留，不會以亮度作全圖判斷，因此不會將白色主體直接刪除。

若背景紋理被模型以高 alpha 誤留，低信心條件不足以清除。新增第二層「外圍調色盤 + 最大主體連通區」清理：以最大的不透明遮罩連通區識別主要人物或物件，再清除不屬於主體且色彩符合外圍背景調色盤的獨立殘留。此步驟用於去除水彩、紙張紋理等大塊及碎點背景，避免以全圖顏色閾值傷及主體內部。

當紋理色塊的色彩變化過大而無法符合外圍調色盤時，第三層清理以最大不透明連通區作為主體，再刪除完全位於主體範圍之外、面積未達主體 34% 的分離遮罩區。它不再以邊界是否有足夠彩色樣本作為前置條件，避免淡色或白底水彩背景讓此規則完全略過；主體相連的輪廓與細節不會被處理。

預設新畫布改採淡米白 `#F7F2E8`，以提高透明缺口、白描邊及殘留背景的視覺辨識；既有工作檔與既有畫布不會被自動覆寫。
