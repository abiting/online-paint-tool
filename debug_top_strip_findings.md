# 頂部黑色列排查紀錄

- 已在已發布網站 `https://painttool-uwbnkjhm.manus.space/` 載入完整頁面，AbiPaint 工具列與內容均存在。
- 瀏覽器實際擷取畫面顯示工具列上方仍有一條深色區域，因此需繼續檢查運行時 DOM 幾何與根節點／外部注入元素。
- 已確認繁中與英文入口的重複 `head` 標籤已修正，完整建置與 TypeScript 檢查通過。

排查結果：Google Analytics 區塊後方誤留了純文字 `...`。瀏覽器將此無效的 head 文字轉入 body，顯示為左上角三個點並建立一行高度，因而把工作台整體往下推並露出深色背景。已自雙語入口移除。

最新開發版驗證：移除後，頁面最上方即為 AbiPaint 頂部工具列，沒有三個點或額外文字行；後續將以發佈版本再次核對。

匯出對齊診斷：已透過實際工作台建立獨立測試工作檔，方形預覽位於畫布座標 x=385、y=225、190×190；文字卡預覽位於 x=293、y=288.5，表示文字與方形均以畫布中心 x=480、y=320 對齊。後續需直接擷取同一案例的 PNG 輸出並比對像素邊界，確認 Canvas 繪製是否保留文字卡的 4px 水平、2px 垂直內距與內容基線。

可視化驗證：關閉工具面板後，工作台預覽中的方形與兩個重疊文字卡均以畫布中心對齊；預覽的文字卡外框寬度明顯大於方形，這是文字內容自然寬度與圖形寬度不同所致，非物件座標偏移。下一步須用同一工作檔重新匯出並比較實際像素邊界，而非以被工具面板遮蔽的截圖量測。

最終像素驗證：測試文字在工作台預覽的可見字形範圍為 y=297.54 至 346.07，修正後 PNG 為 y=298 至 347，誤差小於 1 個畫布像素；方形仍保有相同的 x=385、y=225 物件座標與中心旋轉模型。修正改以 Canvas `textBaseline="top"` 配合 CSS 行框前導量（font bounding ascent 與 actual ascent 的差額）推導字形頂端，避免以 alphabetic baseline 導致的垂直位移。

跨瀏覽器匯出驗證：html2canvas 無法解析專案中既有的 OKLCH 色彩，已改用 `html-to-image` 的原生 SVG DOM 序列化。實際點選 PNG 後，工作台已成功顯示「匯出前預覽」對話框；預覽影像與正式下載使用同一份產生的資料網址，因此外部瀏覽器不再以另一套 Canvas 文字度量重新排版素材。

互動驗證：在工作台實際開啟「匯出影像」選單並選擇 PNG 後，出現「匯出前預覽」與「取消／確認匯出」按鈕，確認 PNG 不會在未確認前直接下載。

空白畫布驗證：先選取文字素材後點擊畫布空白區，DOM 量測顯示文字圖層已無選取類別、控制點數量為 0，右側欄恢復「選取圖片、圖形或文字後可調整」提示，確認取消選取事件已生效。

逐物件置中測試：在最新開發版新增一個 52 px 的「在這裡輸入文字」物件，已觸發渲染後的可視像素邊界校正；目前工具設定面板覆蓋畫布，需在關閉面板後再量測可視中心。

關閉工具設定面板後的實測：畫布中 52 px 新建文字的可視字形中心約落在 y＝418，測試方形中心約落在 y＝419；逐物件像素邊界校正已使兩者落於同一視覺中心。

多物件工作區驗證：目前測試畫布同時保留方形與三個文字物件，可用於後續檢查不同文字內容、字級與對齊狀態。

預覽文案驗證：實際選擇 PNG 後，「匯出前預覽」對話框僅顯示標題、預覽影像與「取消／確認匯出」按鈕，原先的構圖確認說明文字已移除。

工作區互動驗證：在 `.canvas-shell-outer` 下方的深色格線區觸發 pointerdown 後，選取類別數量由 1 變為 0，表示外圍工作區可清除素材選取。

字型嵌入測試：匯出渲染已取消 `skipFonts`，使序列化副本嵌入當前已載入的字型；匯出前預覽可正常開啟，待以乾淨預覽截圖確認輪廓與置中效果。
