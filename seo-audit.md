# AbiPaint GEO／SEO 初步稽核

稽核日期：2026-08-25

## 初始稽核發現

公開首頁可被文字擷取到部分工作台與頁尾內容，但「使用說明」與「開發者」主要位於預設關閉的浮動面板，搜尋引擎與 AI 摘要系統不應只依賴這類互動內容。原有首頁也沒有產品主題的 `h1`；FAQ 題目是一般文字而非 `h3`。

## 本輪改善與驗證

已新增可直接開啟、無須 JavaScript 的中英文指南：`/guide/` 與 `/en/guide/`。兩頁都含一個產品導向的 `h1`、功能與操作步驟的 `h2`／`h3`、FAQ 的 `h2`／`h3`，以及開發者 `h2`。瀏覽器文字擷取已確認繁中與英文頁完整呈現上述階層與開發者內容。

工作台本身的 AbiPaint 品牌名稱現為 `h1`，既有 FAQ 面板問題現以 `h3` 呈現，並在頁尾加入對應語言的指南連結。首頁 JSON-LD 已擴充為 `SoftwareApplication`、`WebSite` 與 `Person`；指南各自含 `FAQPage`。此外，`robots.txt` 已宣告 sitemap，sitemap 已列出繁中、英文工作台與指南，並提供 hreflang 對應。

## 後續觀察

這些調整強化可索引的內容與語意，但不保證特定關鍵字名次、摘要樣式或 AI 回答引用。發布後可在 Google Search Console 提交 sitemap，並觀察索引狀態、實際查詢與點擊資料，再依搜尋詞彙調整指南內容。
