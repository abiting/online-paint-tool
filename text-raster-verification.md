# 文字點陣化驗證紀錄

## 2026-08-19

已在既有「未命名畫布 3」工作檔執行 PNG 匯出，輸出檔為 `Downloads/未命名畫布 3 (4).png`，尺寸為 960 × 640。檢視結果顯示白色畫布、紅色圓角方形、紅色圓點及中文文字皆完整輸出，文字與工作台中的視覺字形、尺寸及相對位置一致，未出現先前 SVG／跨瀏覽器字型回退造成的位移。

工作台 DOM 檢查亦確認三張文字卡均為非編輯狀態，且每張皆含有 `.text-layer-raster` 圖片；目前量測外框皆為約 253.64 × 42.43 CSS 像素，表示圖片預覽沒有因原始 2 倍解析度而放大。

以雙擊事件等待 100ms 後檢查，文字卡會切換為 `contenteditable="true"` 的原生文字，且點陣圖片會暫時移除，文字內容完整保留。再以實際焦點轉移結束編輯後，文字卡回復為 `contenteditable="false"`，並再次帶有 `.text-layer-raster` 圖片，確認預覽與編輯模式可雙向切換。

文字內容、字型、字重、字級、文字顏色與輪廓相關設定變更時，程式會先清除既有快照，再由監聽流程重新建立點陣素材。
