# 圖層與裁切驗證紀錄

## 初步互動驗證

- 最新繁中工作台可正常載入，右上角圖層面板顯示「圖層 1」與鎖定入口。
- 已可從左側文字工具建立文字素材，文字素材會出現在目前工作區，供後續鎖定行為驗證。
- 圖片裁切介面與後續圖片等比例置入規則仍待使用匯入圖片的完整流程驗證。

## 鎖定圖層驗證

- 鎖定圖層 1 後，既有文字素材的選取框與文字設定面板會自動關閉。
- 再次點選該文字不會恢復選取、拖曳或文字編輯，表示鎖定限制已成功套用至文字素材。

## 多圖匯入測試準備

- 解除鎖定後，圖層鎖定按鈕已回復為可鎖定狀態。
- 匯入按鈕可正常啟動原生檔案選擇流程；後續會以實際圖片確認畫布尺寸不再隨第二張圖改變。

## 匯入測試方式

- 預覽頁的檔案輸入元素存在且接受圖片格式，但自動上傳控制無法定位隱藏輸入欄。
- 已改以瀏覽器內建立測試檔案並派發原生 change 事件的方式驗證匯入流程，成功載入第一張 WebP 測試圖片。

## 多圖匯入驗證

- 第一張 512 × 512 WebP 圖片匯入後，工作區與右側畫布尺寸皆正確更新為 512 × 512。
- 接著模擬匯入 1600 × 900 的大型 SVG 圖片；匯入前畫布尺寸為 512，流程已依後續圖片等比例置入規則執行，不會重設既有畫布。
- 匯入完成後，工作區與右側寬高欄仍顯示 512 × 512，且畫布內同時保留第一張圖片、第二張圖片與原有文字素材。
