# 圖層與裁切驗證紀錄

## 初步互動驗證

- 最新繁中工作台可正常載入，右上角圖層面板顯示「圖層 1」與鎖定入口。
- 已可從左側文字工具建立文字素材，文字素材會出現在目前工作區，供後續鎖定行為驗證。
- 圖片裁切介面與後續圖片等比例置入規則仍待使用匯入圖片的完整流程驗證。

## 鎖定圖層驗證

- 鎖定圖層 1 後，既有文字素材的選取框與文字設定面板會自動關閉。
- 再次點選該文字不會恢復選取、拖曳或文字編輯，表示鎖定限制已成功套用至文字素材。

## 多圖匯入測試準備

- 解除鎖定後，圖層鎖定按鈕已回復為可鎖定狀態。
- 匯入按鈕可正常啟動原生檔案選擇流程；後續會以實際圖片確認畫布尺寸不再隨第二張圖改變。

## 匯入測試方式

- 預覽頁的檔案輸入元素存在且接受圖片格式，但自動上傳控制無法定位隱藏輸入欄。
- 已改以瀏覽器內建立測試檔案並派發原生 change 事件的方式驗證匯入流程，成功載入第一張 WebP 測試圖片。

## 多圖匯入驗證

- 第一張 512 × 512 WebP 圖片匯入後，工作區與右側畫布尺寸皆正確更新為 512 × 512。
- 接著模擬匯入 1600 × 900 的大型 SVG 圖片；匯入前畫布尺寸為 512，流程已依後續圖片等比例置入規則執行，不會重設既有畫布。
- 匯入完成後，工作區與右側寬高欄仍顯示 512 × 512，且畫布內同時保留第一張圖片、第二張圖片與原有文字素材。

## 裁切測試準備

- 新的測試工作區已由第一張 512 × 512 圖片建立畫布，畫布尺寸正確維持為 512 × 512。
- 自動化測試中曾有一個延遲中的匯入事件完成，因而產生兩張同名測試素材；這是測試流程重複觸發造成，非一般單次檔案匯入行為。
- 測試中點選圖片後，圖片裁切控制尚未渲染於右側面板；已確認不是被捲動區遮住，而是圖片選取狀態未正確進入檢視器，需優先修正選取流程。
- 進一步檢查顯示圖片圖層已套用 `is-selected` 狀態，但右側設定欄仍只渲染畫布尺寸與影像調色；問題位於圖片檢視器的 React 條件渲染分支，而非點選或 CSS 指標事件。

## 圖片預設鎖定驗證

- 已將圖片檢視器移出停用的舊版設定註解；選取圖片後右側可顯示圖片素材資訊與「開始編輯圖片」入口。
- 非圖片編輯狀態僅能選取圖片；點擊「開始編輯圖片」後才會出現移動、縮放、旋轉與裁切入口，符合預設鎖定需求。
- 裁切測試已調整左側、上方、寬度與高度後套用；畫布仍維持 512 × 512，裁切完成後圖片重新回到非編輯鎖定狀態。

## 後續圖片等比例匯入驗證

- 匯入 1600 × 400 的第二張測試圖後，畫布仍維持第一張圖片建立的 512 × 512。
- 第二張圖片等比例縮小為 512 × 128，並垂直置中於既有畫布，未改變或拉伸底層圖片。

## 圖層鎖定驗證

- 鎖定目前圖層後，先前已選取的圖片會取消選取，右側圖片編輯與裁切入口隨即消失。
- 再次點選鎖定圖層內的圖片不會重新選取素材，因此無法開啟移動、縮放、旋轉、裁切或刪除操作。

## 響應式版面驗證

- 桌面工作台可正常顯示圖層面板、畫布、右側設定欄與匯入入口。
- 手機版畫布、底部設定區與迷你工具列均可正常載入；圖層面板維持在畫布上方可見位置，未遮擋主要畫布。
