# 手機抽屜驗證紀錄

- TypeScript 檢查已通過：`pnpm run check`。
- 390 × 844 手機視窗中，底部設定面板的拖曳把手固定可見，畫布下緣會為抽屜保留可視空間。
- 抽屜高度由 CSS 變數控制；拖曳期間取消過場，放開後低於最小閱讀高度時收合至 72px。
- 桌面版未套用抽屜 CSS，仍使用既有的固定右側設定面板。

## AbiPaint 桌面工具驗證紀錄

- 1280 × 720 桌面視窗中，AbiPaint 品牌與左側「畫筆／圖形／文字」功能列均可見，右側維持畫布尺寸與影像調整。
- 390 × 844 手機視窗中，左側功能列與中央浮動調整器均隱藏，僅保留解析度調整、匯入／匯出與底部設定抽屜。

## 創作工具互動驗證紀錄

- 桌面預覽驗證通過：畫筆啟用時左側圖示保持高亮，中央設定器可開啟。
- 透過實際滑鼠事件驗證，中央設定器可在工作區內拖曳定位。
- 文字工具啟用後直接建立並選取文字圖層，中央面板顯示文字內容、顏色與 13 種字體選項。
- 雙擊畫布上的文字後，圖層會進入可編輯狀態；手機版仍只顯示解析度調整與底部抽屜。

## 工具互動與字體修正驗證紀錄

- 單擊左側「文字」會建立並選取一個新文字圖層，設定器保持關閉；雙擊同一工具會重新開啟文字設定器。
- 單擊文字圖層可選取並拖曳移動，設定器不會自動打開；雙擊文字可進入原地編輯並開啟設定器。
- 字體選單顯示 17 個純名稱選項，先依中文筆畫排列，再依英文名稱 A–Z 排列，包含 Times New Roman、標楷體、新細明體與 Arial。
- 桌面版與 390 × 844 手機版畫面均正常；手機版仍維持解析度工具與底部抽屜，未顯示桌面創作功能列。

## 素材互動與右側欄位驗證紀錄

- 新增圖形後會立即顯示對應設定器；設定器可拖入右側欄位區域。
- 圖形可選取、拖曳移動；新畫筆筆觸會以獨立圖層呈現，可選取、拖曳、刪除與輸出。
- 文字、圖形與筆觸拖曳時會使用與圖片相同的畫布邊緣與中心貼合導引線。
- 右側欄位已移除背景細線與 CAL 標記；解析度縮放開關會顯示目前生效方式。桌面與手機預覽均正常。

## 角點縮放與文字中心驗證紀錄

- 右上與右下角控制點皆可將圖形等比例縮放；實測圖形寬高會隨右側兩角操作正確變更。
- 新增文字與文字中央對齊改用實際渲染寬高計算；畫布座標的中心誤差在測試中小於 1px。
- 等比例縮放圖片保留開關本身，已移除下方多餘狀態說明；390 × 844 手機版抽屜與解析度工具畫面正常。

## 產品說明與版權頁尾驗證紀錄

- 工作區最底部已加入低調的 ABOUT ABIPAINT 產品說明，文字採暖紙灰色與 9px 小字級，不干擾畫布操作。
- Copyright © 2026 與「阿比丁的第二個家」連結並列於頁尾；連結指向 https://abiting.cc。
- 1280 × 720 桌面與 390 × 844 手機全頁預覽均可呈現說明及版權資訊，未覆蓋畫布或手機設定抽屜。

## SEO 介紹收合驗證紀錄

- 產品介紹 HTML 仍保留於工作區頁尾結構中，但可視高度已收合為 0px 並以 overflow 隱藏。
- 1280 × 720 桌面與 390 × 844 手機預覽均不再顯示介紹內容，也不再壓縮畫布可用空間。
- Copyright © 2026 與阿比丁的第二個家連結持續以低調頁尾形式可見。
- 已改用絕對定位、1px 寬度與 0px 高度的收合方式；重新檢查桌面預覽後，產品介紹不再顯示或佔用頁尾空間。

## FAQ 獨立小視窗驗證紀錄

- FAQ 觸發器位於桌面左側文字工具下方；開啟後使用固定浮動小視窗（460 × 620px 上限），不受 74px 工具欄寬度影響。
- 互動驗證確認 FAQ 可開啟、問答可展開、並可再次關閉；面板處於畫布上方的獨立定位層。
- 390 × 844 手機預覽不顯示桌面 FAQ 工具列或小視窗，既有解析度工具、畫布與版權頁尾維持正常。

## 貓咪 Logo 驗證紀錄

- 使用者提供的貓咪 Logo 已置於 AbiPaint 標題左側，採 100% 圓角與 object-fit: cover 呈現。
- 1280 × 720 桌面預覽確認 Logo 與品牌標題對齊；390 × 844 手機預覽確認 Logo 未擠壓匯入、復原與匯出控制。
- 圖片替代文字已設定為 `AbiPaint`。

## WebP Logo 與 Banner 驗證紀錄

- 512px `favicon.webp` 已作為標題左側圓形 Logo 與瀏覽器 favicon；Logo 使用 100% 圓角並保留 alt=`AbiPaint`。
- `banner.webp` 已置於工作區頁尾上方，採 5px 圓角；1280 × 720 桌面與 390 × 844 手機預覽均正常顯示。
- 1024px `favicon-1024.webp` 已上傳為備用資產，暫不顯示於介面。

## FAQ Banner 位置調整驗證紀錄

- 工作台頁尾已移除 Banner，桌面與 390 × 844 手機預覽均恢復為畫布後接精簡版權列的結構。
- Banner 已置於 FAQ 使用說明小視窗的問答內容下方，使用 5px 圓角；替代文字為「線上圖片尺寸修改器」。
- 品牌 Logo 的替代文字維持為 `AbiPaint`。

## 手機迷你工具欄驗證紀錄

- 390 × 844 手機預覽顯示畫布左上方的迷你工具欄，包含拖曳把手、畫筆、圖形與文字快捷鍵，且不會遮擋底部設定抽屜。
- 1280 × 720 桌面預覽不顯示手機迷你工具欄，保留原有左側創作工具列與右側解析度面板。
- 手機迷你工具欄加入設定按鈕；互動驗證確認點擊畫筆建立素材後會開啟設定面板，關閉後再點設定按鈕也可重新開啟。
- 修正舊有手機斷點將創作設定面板設為隱藏的規則後，實際互動驗證確認面板顯示為 `block`，尺寸為 366 × 354 px，位於底部抽屜上方。

## 筆觸選取模式驗證紀錄

- 390 × 844 手機預覽顯示迷你工具欄的選取／移動按鈕、畫筆、圖形、文字與設定入口。
- 1280 × 720 桌面預覽顯示左側新增的選取工具，並與畫筆、圖形與文字工具維持一致排列。

## 工具入口可理解性驗證紀錄

- 390 × 844 手機預覽顯示垂直握把作為工具欄拖曳把手，與相鄰的四向箭頭選取圖示明確區隔；沒有素材時選取按鈕呈暗淡停用狀態。
- 1280 × 720 桌面預覽顯示選取與設定入口；空白畫布時皆維持暗淡停用，避免誤觸。

## 圖形建立流程驗證紀錄

- 桌面與手機圖形工具的開啟流程已不再呼叫預設方形建立。
- 方塊、圓形、星形、愛心、三角形與五邊形按鈕仍各自保留建立素材事件。

## 雙語路由與設定欄驗證紀錄

- `/` 繁中與 `/en` 英文工作台皆可正常載入；頂部語系入口顯示相反語系名稱。
- 英文路由顯示英文預設文件名稱、工具列、畫布尺寸與影像調整控制項。
- 桌面與手機右側設定欄僅保留 `CANVAS`、`IMAGE ADJUSTMENTS` 分類標籤，已移除重複的大標題。
- 工作區狀態標籤已改為「本工具由阿比丁開發製作」及其英文版本。
- 繁中右側分類標籤已改為「畫布尺寸」與「影像調色」；英文 `/en` 維持英文分類標籤。

## 圖片控制點固定尺寸驗證紀錄

- 圖片縮放與旋轉控制點加入工作區縮放的反向比例補償，因此畫布縮放不會改變其螢幕上的視覺與觸控尺寸。
- 旋轉連接線、旋轉把手與角度標籤的間距也同步套用補償，維持固定的操作距離。
- TypeScript 檢查已通過；桌面工作台預覽確認版面與既有設定欄未出現回歸。

## 圖形控制點獨立覆蓋層驗證紀錄

- 使用者截圖中的控制點來自 SVG 圖形素材；其原有座標會被圖形的寬高拉伸，造成把手隨素材比例變形。
- 已將圖形的八個縮放控制點、旋轉控制與角度標籤移至獨立 HTML 覆蓋層，並以反向比例補償工作區縮放。
- TypeScript 檢查通過；桌面預覽確認工作台仍正常渲染。

## 開發署名 Facebook 連結驗證紀錄

- 繁中 `/` 顯示「本工具由阿比丁開發製作」，其中「阿比丁」連往 https://www.facebook.com/shimokitazawa.news/。
- 英文 `/en` 顯示「Developed by Abiting」，其中「Abiting」連往相同 Facebook 粉絲專頁。
- 兩個路由的署名連結皆保留既有工作區字級與辨識度，並在桌面預覽中正常顯示。

## 原生文字複製快捷鍵驗證紀錄

- Ctrl／Command + C 在反白一般網頁文字時，不再被畫布文字卡複製功能攔截，因此維持瀏覽器原生複製行為。
- 只有未反白一般文字且已選取畫布文字卡時，才會執行工具內的文字卡複製。
- TypeScript 檢查已通過；桌面工作台預覽無版面回歸。

## 圓形預設尺寸驗證紀錄

- 新增圓形時，初始寬度與高度皆為 190px，因此會直接建立正圓。
- 使用者後續仍可透過邊緣或角落控制點調整為橢圓。
- TypeScript 檢查與桌面預覽均已通過。

## 三種筆刷工作台驗證紀錄

- 畫筆設定已擴充為油線筆、鉛筆與毛筆三種選項；鉛筆採較細、低不透明度的素描筆觸，毛筆加入沿拖曳方向延伸的筆鋒。
- TypeScript 檢查通過，桌面與手機工作台預覽均可正常載入，未出現工具列或設定抽屜版面回歸。

## 圖層面板互動驗證紀錄

- 工作區右上角的圖層面板可顯示目前層數與 5 層上限，新增圖層後會立即建立並選取新圖層。
- 每一列圖層均提供獨立鎖定按鈕；切換後按鈕會正確更新為解除鎖定狀態。
- 以實際互動連續建立至圖層 3，清單排序、當前選取層與 `3 / 5` 計數均正確更新。
- 進一步建立至圖層 5，面板正確顯示 `5 / 5`，新增圖層按鈕已切換為停用狀態。

## 平滑筆刷核心載入驗證紀錄

- 重新啟動開發伺服器後，平滑筆刷核心、三種筆刷選項與右上角圖層面板均可正常載入。
- 生產建置與 TypeScript 檢查均已通過，未再出現先前熱更新訊息所指的 CSS 建置錯誤。
- 瀏覽器以程式合成的 PointerEvent 不會被畫布視為真實繪製手勢，因此未用此方式評估筆觸外觀；使用者實際滑鼠與觸控手勢仍由既有畫布指標流程處理。
- 獨立筆尖版本的油線筆、鉛筆與毛筆選項可與右上角圖層面板同時正常顯示。
- 畫布已綁定完整的 React 指標起始、移動、結束與取消處理器；毛筆選項可在設定面板中正常切換。
- 直接透過畫布的實際指標處理流程建立毛筆筆觸後，畫布與 SVG 疊圖均新增筆觸；畫面確認線條連續、無逐段尖角鋸齒，且末端會縮收。
- 以相同的畫布指標流程切換至鉛筆後，筆觸數量正確由 1 增加至 2，確認鉛筆可獨立建立筆觸而不影響既有毛筆內容。
- 鉛筆加深與毛筆微透參數更新後，重新載入的工作台和筆刷設定面板均可正常載入。
- 調整後的毛筆可透過實際畫布指標流程建立筆觸，筆觸資料成功寫入目前圖層。

## 純文字筆刷選擇器驗證紀錄

- 油線筆、鉛筆與毛筆的示意圖已移除，三個選項以純文字按鈕呈現。
- 已選取的筆刷仍保留朱砂色邊框與背景狀態，且不再以圖像暗示實際筆觸外觀。

## 工具面板標題簡化驗證紀錄

- 繁中畫筆面板標題已由「畫筆設定」改為「畫筆」，且設定面板可正常開啟與關閉。
- 英文畫筆面板標題已由 `Brush settings` 改為 `Brush`，三個筆刷文字選項與原有操作控制均正常顯示。

## 楷體網路字型來源

- Google Fonts 已確認提供繁體中文楷體 `LXGW WenKai TC` 的 400 與 700 字重，可透過 `https://fonts.googleapis.com/css2?family=LXGW+WenKai+TC:wght@400;700&display=swap` 載入，以取代依賴裝置安裝狀態的 `DFKai-SB`。

## 英文文字面板驗證紀錄

- 英文文字面板已顯示 `Text`、`Text content`、`Font`、`Text color`、`Font size`、`Text opacity`，新建文字的預設內容為 `Type here`，楷體選項顯示為 `Kai`。
- 新增文字後的成功提示仍顯示繁中「文字已加入畫布」，需在發布前一併改為雙語提示。
- 新增文字的提示已改為 `Text added to canvas`；實測仍顯示 `Noto Serif JP`，需完成其移除後再發布。
- 已重新載入英文文字面板確認：`Noto Sans JP` 與 `Noto Serif JP` 均不在選單中，保留 `Kai` 楷體與其餘不同風格字型；新增文字提示維持英文。

## 響應式與英文圖層面板驗證紀錄

- 手機版仍可同時顯示迷你工具列、畫布與底部設定抽屜；右上角圖層面板未遮擋其主要控制。
- 英文 `/en` 路由正確顯示 `Layers`、`Layer 1`、`Add layer` 與 `Lock layer` 的對應英文文案。

## 匯出影像選單驗證紀錄

- 頂部原本分開的 PNG、JPG 按鈕已整合為單一「匯出影像」入口；桌面下拉選單可顯示 PNG、JPG、PDF 三種格式。
- 手機版頂部僅保留單一朱砂色匯出入口，不再因兩個格式按鈕擠壓工具列。
- TypeScript 與生產建置通過；PDF 匯出沿用目前畫布合成影像建立 PDF 下載檔案。

## 快速裁切控制驗證紀錄

- 裁切模式會以完整裁切框預設選取圖片，並顯示四角、四邊與中央移動控制點；不需重新自行框選範圍。
- 實測拖曳右下角控制點會即時縮小裁切範圍，套用後可產生實際裁除像素的 PNG 資料來源，並保留一般圖片移動、縮放與旋轉能力。
- PDF 匯出按鈕已以實際畫布測試，顯示「PDF 已匯出」完成提示。

## 開發者視窗公開圖片驗證紀錄

- 已確認 `https://coai.abiting.cc/wp-content/uploads/2026/01/watercolor_style.webp` 可公開載入，原始尺寸為 2304 × 1856px。
- 實際開啟開發者視窗後，圖片位於代表作清單下方，並具備替代文字 `AbiPaint 免費修圖`；面板可捲動瀏覽完整內容。
- 英文開發者視窗實測顯示無人稱介紹文字，並依英文介面僅列出英文代表作名稱；圖片與外開連結均可正常呈現。
- 桌面左側的 FAQ 與 Developer 已由創作工具下方移至具有分隔線與留白的獨立輔助資訊區。
- 實際開啟繁中開發者視窗後，標題列右側可見 Facebook 與 LinkedIn 圖示連結，兩者位於關閉按鈕左側，並保留明確的無障礙標籤與外開設定。
- 桌面預覽已確認 Ko-fi 外部元件成功載入，Donate 按鈕固定顯示於左下角；頁面 DOM 可辨識其 Donate 按鈕，並採用使用者指定的綠色設定。
- 本機專案自動還原已在桌面預覽驗證：重新載入後只顯示一次「專案已載入」通知，頂部的專案操作入口可正常顯示，不再發生重複還原迴圈。
- 本機自動儲存已實測：將文件名稱改為「複雜設計備份測試」後重新載入，名稱仍能正確還原，確認 IndexedDB 暫存流程可用。
- 專案選單已實測可開啟「匯出專案／匯入專案」兩項操作；匯出操作後顯示「專案檔已匯出」通知，確認手動備份入口可用。
- 專案匯入選項已可由頂部專案選單觸發隱藏的檔案選擇器，準備使用測試匯出的 `.abipaint` 檔驗證還原流程。
- 測試匯出的「複雜設計備份測試.abipaint」已成功重新上傳，顯示「專案已載入」通知並還原文件名稱，確認手動備份檔匯入流程可用。
- 頂部入口已改為「檔案」，選單順序確認為先「匯入專案」、再「匯出專案」；手機版初始預覽顯示選取模式為作用中，未預設啟用筆刷繪製。
- 英文版頂部入口已驗證為 File，選單順序為 Import project、Export project，與繁中版操作順序一致。
